export enum Everything {
    DATES = "Dates",
    SOURCES = "Sources",
    LANGUAGE = "Language"
}

export enum TopHeadlines {
    COUNTRY = "Country",
    SOURCES = "Sources",
    CATEGORY = "Category",
}

export interface SideBarProps {
    
}